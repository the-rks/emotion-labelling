/* Paste your Apps Script web app URL here after deploying it.
   It ends in /exec — not /dev.
   Leave it empty to run the task in local-only mode while you test the
   interface (responses stay in the browser and can be exported as CSV). */
const CONFIG = {
  ENDPOINT: "https://script.google.com/macros/s/AKfycbwI-TniOtEfuNd87b11qBW0KT7zV76LeKS0amY15pJuXWSooI9R7vhjDLxJaJsBuHyEKg/exec",
  TWEETS_PER_PARTICIPANT: 5,
  STUDY_NAME: "Emotion labeling study"
};
